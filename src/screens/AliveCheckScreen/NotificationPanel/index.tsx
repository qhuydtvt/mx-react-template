import React from 'react';
import {
  Typography,
} from '@material-ui/core';

export default (): JSX.Element => (
  <div>
    <Typography variant="h6">
      Notification
    </Typography>
  </div>
);
